# Python
__pycache__/
*.pyc
.venv
backend/storage/
backend/logs/

# Node
node_modules/
frontend/dist/

# Env
.env
.env.local

# Editors
.DS_Store

# Superpowers scratch (SDD workspaces, brainstorming sessions)
.superpowers/

# Quality-harness real-audio test corpus (~25MB of WAV/MP3 fixtures) and its
# generated run output — kept on disk for reuse, never committed (binary
# audio/MIDI would permanently bloat repo history). See
# backend/scripts/quality_harness/run_baseline.py for provenance notes.
backend/scripts/quality_harness/assets/
backend/scripts/quality_harness/output/

# MAESTRO ground-truth eval: fetched (audio, MIDI) clip pairs and generated
# run output — never committed, same reasoning as quality_harness/ above.
# See backend/scripts/ground_truth_eval/fetch_maestro_clips.py.
backend/scripts/ground_truth_eval/assets/
backend/scripts/ground_truth_eval/output/
