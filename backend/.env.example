# Required only for Spotify-link input (resolved via Spotify's Web API, then
# matched and downloaded through YouTube). Create an app at
# https://developer.spotify.com/dashboard to obtain these. File-upload and
# YouTube-link input work without these being set.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
